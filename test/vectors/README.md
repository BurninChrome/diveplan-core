# Conformance vectors

Known-good input/output pairs for the Bühlmann model, **derived from published
decompression theory rather than from this library**. They state how the model
should behave, so they remain valid across a rewrite — a reimplementation in
another language can be checked against exactly these files.

## Why these exist

`test_buhlmann.c` checks the library against its own formulas restated in the
test. That catches typos, but it cannot catch a wrong formula or a wrong
constant: both sides would agree. Three separate review passes of this codebase
found defects that the 202 existing assertions pass straight over.

These vectors are generated by an independent implementation of the published
equations, so agreement means something.

## The two mechanisms, and why they are different

**Conformance vectors** (`*.tsv`, checked by `test_vectors`) say how the model
*should* behave. They do not change when a bug is fixed — the bug was always a
deviation from them.

**The regression baseline** (`../baseline.sha256`, checked by `../baseline.sh`)
records what the model *currently* outputs across the 39 real dive logs in
`../xml/`, defects included. It only detects change.

Keeping these apart matters. A golden file recorded from buggy output slowly
becomes the specification if nobody remembers what it was for; that is how a
"reference" ends up enshrining a defect. The vectors are the authority, the
baseline is a tripwire.

## Files

| File | Checks |
|---|---|
| `alveolar.tsv` | `ventilation()` across ambient pressures, all three RQs, several gas fractions |
| `haldane.tsv` | Constant-pressure loading at 0/1/2/3 half-times and saturation, on- and off-gassing |
| `schreiner.tsv` | Ramp loading, descent and ascent, including `rate = 0` reducing to Haldane |
| `ceiling.tsv` | Combined-gas ceiling by the Bühlmann rule (`a`, `b` blended by partial-pressure share) |
| `gradient-tolerance.tsv` | GF-adjusted tolerated ambient pressure at GF 1.00 → 0.30 |
| `ndl.tsv` | No-decompression limit, by bisection on the ceiling crossing |
| `zh-l16-derivation.tsv` | ZH-L16 coefficients against `a = 2/∛t½`, `b = 1.005 − 1/√t½` |

## Expected failures

`expected-failures.txt` lists, per file, how many vectors currently deviate and
why, each with a pointer to its write-up in `.okf/findings/`. CI stays green on
known defects and fails on new ones.

The runner also **fails when there are fewer deviations than allowed**. That is
deliberate: fixing a defect must update this file in the same change, which is
how the fix proves it did what it claimed.

## Running

```sh
make check                      # both suites, via automake
./test_vectors vectors          # vectors only, from the test/ directory
../test/baseline.sh check       # regression baseline
../test/baseline.sh record      # regenerate it — deliberately, see below
```

## Regenerating

Vectors:

```sh
python3 gen_vectors.py
```

The generator must never import, link against, or shell out to `libbuhlmann`.
If it ever does, the vectors stop being independent and this whole directory
becomes decoration. It parses the constant tables out of `src/*.c` — that is
intentional, so a table edit is reflected, but note it means the tables
themselves are checked only against the derivation formula, not against the
generator.

Baseline: only alongside an approved change that legitimately moves the output,
in the same commit. Never to turn a red build green.

## Precision

Tolerance is `1e-6` for the equation vectors and `0.5` minutes for NDL, which is
coarser than the model's own precision and deliberately so — the NDL search is
iterative and its granularity is not a property worth pinning.

Half-times are compared at `1e-4` rather than tighter, because the constant
tables store `f`-suffixed literals in `double` fields, so every value is
float-rounded then widened: `38.3` reads back as `38.2999992371`. That is a
separate cleanup and conflating it with wrong coefficients would muddle the
counts.

## Citations

[1] Erik C. Baker, *Understanding M-values* — `doc/m-values_en.pdf`.
[2] Bühlmann coefficient derivation and the published ZH-L16C table.
[3] Full method and its limits:
    `.okf/decisions/2026-07-25-bundle-verification-method.md`.
